import React from 'react';
import { Wrapper, Text, SafeAreaView } from './styles';

const Header = () => (
  <SafeAreaView>
    <Wrapper>
      <Text>to.</Text>
      <Text bold>do</Text>
    </Wrapper>
  </SafeAreaView>
);

export default Header;
